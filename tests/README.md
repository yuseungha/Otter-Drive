# System tests

ROS package unit tests remain beside each package under `src/jetson/*/test` so
`colcon test` discovers them. This directory is reserved for cross-package,
recorded-video, and hardware smoke tests.
