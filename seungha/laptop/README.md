# 노트북 워크스페이스

이 폴더에는 개인 노트북과 WSL Ubuntu 22.04에서 개발·검증한 자료를 둡니다.

- `ros2_ws`: ROS 2 Humble 소스 워크스페이스
- `windows_launchers`: Windows에서 로컬 시험 또는 Jetson 원격 실행을 여는 도구

`build/`, `install/`, `log/`, `.venv/`, 모델, 영상과 장비별 승인 파일은 Git에
포함하지 않습니다.
